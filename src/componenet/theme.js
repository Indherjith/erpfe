// theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FFFFFF",
    },
    secondary: {
      main: "#004E69",
    },
    background: {
      default: "#FFFFFF",
    },
  },
  typography: {
    // fontFamily: "Lato",
  },
});

export default theme;
